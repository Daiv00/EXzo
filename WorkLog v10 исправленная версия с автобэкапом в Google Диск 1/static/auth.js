let mode="login";const $=s=>document.querySelector(s);
async function csrf(){const r=await fetch("api/csrf");return (await r.json()).token}
document.querySelectorAll(".tab").forEach(b=>b.onclick=()=>{mode=b.dataset.mode;document.querySelectorAll(".tab").forEach(x=>x.classList.toggle("active",x===b));$("#title").textContent=mode==="login"?"С возвращением.":"Создай аккаунт.";$("#subtitle").textContent=mode==="login"?"Войди, чтобы продолжить учёт своих смен.":"После регистрации твои смены будут отделены от данных других пользователей.";$("#submit").textContent=mode==="login"?"Войти →":"Создать аккаунт →";$("#password").autocomplete=mode==="login"?"current-password":"new-password";$("#error").textContent=""});
$("#authForm").onsubmit=async e=>{e.preventDefault();$("#error").textContent="";try{const token=await csrf();const r=await fetch("api/"+mode,{method:"POST",headers:{"Content-Type":"application/json","X-CSRF-Token":token},body:JSON.stringify({username:$("#username").value,password:$("#password").value})});const d=await r.json();if(!r.ok)throw Error(d.error||"Ошибка");location.href=""}catch(err){$("#error").textContent=err.message}};
